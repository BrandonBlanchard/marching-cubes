import React from 'react';

const Terrain = props => {
    const {
        sampleSpacing,
        width,
        depth,
        height
    } = props;
    const sampleOffset = sampleSpacing / 2;
    
    return (
        <div></div>
    );
};

export default Terrain;